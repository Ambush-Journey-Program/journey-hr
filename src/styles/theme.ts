import {
  contrasts,
  auxiliary,
  gradients,
  brandColors,
  button,
  shadows,
} from './colors';
import { font } from './fonts';

export const theme = {
  color: {
    brandColors,
    auxiliary,
    contrasts,
    gradients,
    button,
    shadows,
  },
  font: font,
};
