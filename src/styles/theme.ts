import { contrasts, auxiliary, gradients, brandColors, button } from './colors';
import { font } from './fonts';

export const theme = {
  color: {
    brandColors,
    auxiliary,
    contrasts,
    gradients,
    button,
  },
  font: font,
};
