import {
  contrasts,
  auxiliary,
  gradients,
  brandColors,
  button,
  icon,
} from './colors';
import { font } from './fonts';
import { shadows } from './shadows';
import { spacings } from './spacing';

export const theme = {
  color: {
    brandColors,
    auxiliary,
    contrasts,
    gradients,
    button,
    icon,
  },
  shadows,
  font,
  spacings,
};
