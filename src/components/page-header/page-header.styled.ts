import styled from 'styled-components';
import { BackButton } from '../ui/back-button';

export const Wrapper = styled.div`
  margin-top: 6px;
`;

export const StyledBTn = styled(BackButton)`
  padding-left: 0;
  margin-bottom: 12px;
`;
