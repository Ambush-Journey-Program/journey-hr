export interface I{{> ComponentName}}Props {
  {{#if includeSkeletonLoader }}
  isLoading?: boolean
  {{/if }}
  label: string
}
{{#if includeContainer}}

export interface I{{> ComponentViewName}}Props {
  {{#if includeSkeletonLoader }}
  isLoading?: boolean
  {{/if }}
  label: string
}
{{/if}}
