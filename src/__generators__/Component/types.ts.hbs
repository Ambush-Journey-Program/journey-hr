export type {{> ComponentName}}Props = {
  {{#if includeSkeletonLoader }}
  isLoading?: boolean
  {{/if }}
  label: string
}
{{#if includeContainer}}

export type {{> ComponentViewName}}Props = {
  {{#if includeSkeletonLoader }}
  isLoading?: boolean
  {{/if }}
  label: string
}
{{/if}}
