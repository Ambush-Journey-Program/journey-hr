{{#if includeSkeletonLoader }}
import { {{> SkeletonName}} } from './{{> SkeletonFileName}}'
{{/if}}
import { Wrapper } from './{{> ComponentFileName}}.styled'
import { {{> ComponentViewName}}Props } from './types'

{{#if includeSkeletonLoader }}
export function {{> ComponentViewName}}({ isLoading, label }:{{> ComponentViewName}}Props){
{{else}}
export function {{> ComponentViewName}}({ label }:{{> ComponentViewName}}Props){
{{/if}}
  {{#if includeSkeletonLoader }}
  if (isLoading) {
    return <{{> SkeletonName}} />
  }

  return <Wrapper>{label}</Wrapper>
  {{else}}
  return <Wrapper>{label}</Wrapper>
  {{/if}}
}
